# Local overrides (gitignored) — same Supabase project as Attendance Management System

spring.datasource.url=${SUPABASE_DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${SUPABASE_DB_USER:postgres.zszwwwpzyepuduielccq}
spring.datasource.password=${SUPABASE_DB_PASSWORD:Carlomercado@123}

# Library tables live in the library schema (Attendance uses public)
spring.jpa.properties.hibernate.default_schema=library
spring.jpa.properties.hibernate.create_namespaces=true
spring.datasource.hikari.connection-init-sql=CREATE SCHEMA IF NOT EXISTS library

# Gmail SMTP (OTP + email notifications)
spring.mail.host=smtp.gmail.com
spring.mail.username=edulibrary67@gmail.com
spring.mail.password=kttaspipdrkzxfby
library.mail.from=edulibrary67@gmail.com
